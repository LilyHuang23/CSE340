<% if (title) { %>
<h1><%= title %></h1>
<% } else {
    res.redirect('/')
} %>
    <!-- # disables this code. 
    you'll replace with mark (—) -->
<%- messages() %>
<form id="classificationForm" action="/inv/addClassification" method="post">
    <p>All field are required</p>
<fieldset>
    <p>Classification Name</p>
    <label for="classification_id">NAME MUST BE ALPHABETIC CHARACTERS ONLY</label><br>
    <input type="text" id="classificationId" name="classification_name" required pattern="[a-zA-Z]+"><br>
   
     <button class="addClassificationBtn"type="submit">Add Classification</button>
</fieldset>
</form>

<% if (title) { %>
<h1><%= title %></h1>
<% } else {
    res.redirect('/')
} %>

<% if (errors) { %>
<ul class="notice">
<% errors.array().forEach(error => { %>
    <li><%= error.msg %></li>
<%  }) %>
</ul>
<% } %>
<%- messages() %>
<h2> Welcome <%- locals.accountData.account_firstname  %></h2>
<p>You are logged in</p>
<a href="/account/updateView/<%- locals.accountData.account_id %>"> Update account information </a>


 <!-- Message Links to Inbox -->
 <h2>Message Center</h2>

   <p>You have <%- unread %> unread messages</p>
<a href="/account/inbox/<%- locals.accountData.account_id %>">Go to inbox</a>

 

<!-- management view -->
<% if (locals.accountData.account_type === 'Employee' || locals.accountData.account_type === 'Admin') { %>
    <h2> Inventory Management </h2> 
    <p><a href="/inv"> Management View </a></p>
    <% } %>
  